# E2E evidence: driver 0.1.0 with Metabase 0.63.1.12

- Date: 2026-07-29
- Metabase: official image `metabase/metabase:v0.63.1.12`
- Image digest:
  `sha256:4f150effd4849b89c428bc98fc1df300792fbc3a3567cc5697e0daa6c13d8ff0`
- Runtime JVM: Eclipse Temurin 25.0.3+9 LTS
- Artifact build baseline: Metabase 0.51.14
- Tested artifact build JVM: Oracle JDK 23.0.1
- ODPS JDBC: 3.10.11, bundled
- Tested JAR SHA256:
  `20f1c6ab6c8700cfd80a126918077a60bde633b1e447a0baca59ee17c457f346`
- MaxCompute project: dedicated real project, identifier omitted

## Results

| Gate | Result |
| --- | --- |
| Plugin registration | Pass |
| Real database creation and authentication | Pass |
| Full schema sync | Pass |
| Synced metadata | 998 tables, 6943 fields |
| Native `SELECT 1 AS e2e_value` | Pass, one row with value `1` |
| MBQL query against a synced real table | Pass, one row |
| Native and MBQL rerun after sync | Pass |

The run used a temporary official Metabase container and application database.
The application database, container, server logs, database connection, and
session token were removed after the run.

The same suite must become a required release gate before release assets
are published. The declared target range remains under verification.
