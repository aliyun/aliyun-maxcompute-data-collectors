#!/bin/bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
java -cp "$DIR/../libs/task-scheduler-1.0-SNAPSHOT-jar-with-dependencies.jar" com.aliyun.odps.datacarrier.taskscheduler.TaskScheduler "$@"